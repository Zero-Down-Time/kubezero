keycloak:
  enabled: false

  istio:
    enabled: false
    gateway: istio-ingress/private-ingressgateway
    url: ""

  metrics:
    enabled: false

postgresql:
  enabled: false

  auth:
    existingSecret: kubezero-auth-postgresql
    username: keycloak
    database: keycloak

  primary:
    persistence:
      size: 1Gi

  readReplicas:
    replicaCount: 0
